/** MCP server implementation name (Claude Code / Desktop) */
export const NUWA_MCP_SERVER_NAME = "nuwa";

/** Project-scoped Claude Code MCP config */
export const NUWA_MCP_CONFIG_FILE = ".mcp.json";

/** Nuwa MCP server metadata */
export const NUWA_MCP_SERVER_INFO = {
  name: "nuwa-reviewer",
  version: "0.1.0",
} as const;

/** Env var Claude Code sets for stdio MCP servers */
export const CLAUDE_PROJECT_DIR_ENV = "CLAUDE_PROJECT_DIR";
